Wir haben für das Projekt nur aus dem Unterricht bekannte Mittel verwendet.

Setup:

Rechtsklick auf das Projekt in Visual Studio --> Properties und anschließend
1. --> C/C++ --> Additional Include Directories --> glut/include mit lokalem Pfad einbinden
2. --> Linker --> Additional Library Directories --> glut/lib mit lokalem Pfad einbinden
